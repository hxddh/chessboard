# 7.6 · 先修会卡死、会丢结果的，再把「点了没反应」这一类修干净

写在 7.5.0 发布之后、动手之前。规矩照旧：每条写**依据**（核过的代码、跑出来的数），
每条有**验收**，发布前补**落地记录**，做到的和没做到的都逐条写。

这一版的依据来自三处：

- 用真 Stockfish 在 Chromium 里把产品从新用户、回头用户、棋谱库用户、开局书用户四个
  角度各走了一遍（1400×900 为主，540×900 走了一遍）；
- 一次代码审计，专找 7.5 在 WebKit 上抓到的那一类问题（点击期间重建 DOM），以及真引擎
  测试没覆盖的路径；
- CI 和发布流水线的实测耗时。

下面每一条后面标了来源：**走查**、**审计**，或 **核过**（在代码里逐行确认过）。

---

## 0 · 结论先行

7.5 之后，引擎相关的主路径都能用，全程没有一条页面报错。但真用起来，有三类问题：

1. **会卡死、会丢结果的**（§1）。开着持续分析时跑棋谱库分析，会永久卡住。精析做到一半，
   点一下「记为变着」，结果一声不吭就没了。对局分析要算 13–34 秒，刷新页面就丢。
   这几条都是用户花了时间、却什么也没拿到。**这是 7.6 的 P0。**
2. **「点了没反应」没有修干净**（§2）。7.5 修的只是「名字没变的第二次 change」。
   真实用户打完名字直接去点按钮时，第一次 change 照样重建按钮上方的内容，实测按钮上移
   16px。持续分析开着时，「新局」按钮每秒上下跳。现在的测试是先 `fill` 再手动派发
   change，永远走不到真实的失焦路径。
3. **主路径上的几处断点和误导人的数字**（§3、§4）。自称新手的人，默认对手反而更强；
   诊断弹窗比窗口高，又滚不动；「进步」显示的其实是失手率。

另外，CI 有 40% 以上的时间花在一个测试写法的 bug 上。发布一次要 3 小时 11 分，其中
2 小时 36 分是两套浏览器引擎串行跑（§5）。

---

## 1 · P0：会卡死、会丢结果的

### 1a 持续分析开着时，棋谱库分析会永久卡住（审计，核过）

**依据**：`liveAllowed()`（app.js:3961）检查了 `analyzing` 和 `engineThinking`，
但没有检查 `libRun`。批量分析开始后，用户任意一次操作都会触发 sync，重新启动
`go infinite`。它排进 exclusive 队列后一直占着锁，后面的 `analyze` 永远等不到。
实测：点一次「‹」，进度停在 17/23，15 秒不动，按钮仍显示「暂停分析」。不开持续分析
的对照组，4 局全部跑完。「再深一遍」同理。

**做什么**：`liveAllowed()` 加上 `!store.session.libRun`。批量分析和「再深一遍」
结束时，如果 `liveOn` 仍为真，就恢复持续分析。

**验收**：真引擎流程测试新增一个场景：先开持续分析，再跑棋谱库分析，中途翻一步棋，
三局都能跑完。不改代码时这个场景是红的。

### 1b 分析中点「记为变着」，精析被悄悄取消（审计）

**依据**：分析进行中，「记为变着」按钮仍然可点（app.js:4381）。点了会改变
`game.pgn()`，于是 4089/4104 直接 return，没有任何提示，已分析的部分也全部丢掉。
实测：精析做到「停止 3/7」时点它，只弹出「已存为变着」。按推断，分析中编辑注释也会
一样。

**做什么**：分析进行中，把会改动棋谱的操作（记为变着、编辑注释）置灰；或者允许操作，
但明确提示「分析已停止」，并保留已完成的部分。优先选前者。

**验收**：e2e：精析进行中，这两个入口不可点。

### 1c 分析结果刷新就丢；精析结果不回写棋谱库（走查）

**依据**：人机对局分析完，刷新页面以后，精准度、曲线、回顾全没了，「分析」按钮又亮了
起来。从「对局历史」载入也一样。原因是 `store.session.analysis` 只存在会话里。
真引擎下，分析 61 半着要 12.9 秒，精析 81 半着要 33.8 秒。另外，对从棋谱库打开的局
点「精析」，结果不回写库：库里的 budget 仍是 200，精准度仍是 77，界面上已经是 76。

**做什么**：
- 分析结果按棋谱（pgn 的哈希）存盘，和自动存档放在一起；载入同一局时直接恢复。
- 从棋谱库打开的局，精析完成后回写那一条的 `an`，逻辑和「再深一遍」相同，包括
  reviseMines。

**验收**：
- e2e：分析后刷新，精准度和标注都还在，引擎的 go 计数不增加。
- 从库里打开一局做精析，库里那一条的 budget 和精准度跟着更新。

---

## 2 · 「点击期间 DOM 被重建」这一类，修干净

### 依据（审计实测，Chromium）

| 位置 | 触发 | 实测位移 |
|---|---|---|
| `#lib-names` 的第一次 change（app.js:8823） | 打完名字直接点按钮，失焦触发 change，`renderLibrary` 对按钮上方的 `#lib-body` 做 replaceChildren | 1100×1000 下各按钮上移 16px（1400 宽时不移动） |
| 持续分析 `#live-line`（app.js:3997–4027） | 每个 rAF 做一次 replaceChildren | multipv=3 时，8 秒内「新局」上下跳 6 次，每次 22px |
| 分析中的 `#pv-line`（4340–4390） | 每一着都整段重建着法 chip 和「记为变着」 | 旧 chip 300ms 内就被替换；Chromium 12 次点击 0 次丢失，WebKit 没有验证 |

7.5 在 WebKit 上确认过：按钮在按下与松开之间挪了位置，这次点击就会丢。

### 做什么

- 棋谱库面板里，名字确认后的提示行挪到按钮下方，或者给它固定高度，让失焦不再挪动按钮。
- `#live-line` 和 `#pv-line` 改成原地更新文字，或者给它们设最小高度，不再每帧整段替换。
- 测试改成真实路径：`type` 完名字直接 `page.click` 按钮，不再手动派发 change；在
  WebKit 上跑。
- 加一条通用断言：在上面三处，被点的按钮从 mousedown 到 mouseup 期间位置不变。

### 验收

- WebKit 上：打完名字直接点「分析」，一次就开始。
- 持续分析开着时，连点「新局」10 次，每次都生效。

---

## 3 · 主路径上的断点（走查）

| # | 问题 | 做什么 |
|---|---|---|
| 3a | 引导里选「我是新手」进入教学，难度留在默认的 `normal`（约 1700）；选「我会下棋」反而是 `easy`（app.js:7446–7454，核过）。上完两课切到人机，面对的是中级 | 选「新手」时同时设成 `beginner` |
| 3b | 评估曲线在所在标签页隐藏时被重绘，画布成了 1×1，拉伸成一整块红色（从棋谱库打开一局，或者在设置页换语言以后） | 标签页变为可见、或窗口 resize 时重画 |
| 3c | 「看诊断」弹窗比窗口高：24 局时卡片高 1025px，窗口 900px。`.modal` 没有 max-height，滚轮滚的是背后的面板，关闭按钮在 y=976，下半部分的开局行点不到 | 所有弹窗统一 max-height 加内部滚动 |
| 3d | 教学或做题模式下，从棋谱库点一局毫无反应，只弹出一条被弹窗模糊背景盖住的 toast。每日计划的最后一步恰好把人留在教学模式 | 和「看那局棋」一样，自动切到双人复盘；toast 的层级放到弹窗之上 |
| 3e | 从诊断里按「第 10 回合」或按母题筛出来的局，打开后停在终局 | 打开时停在出问题的那一手，侧栏切到对局页 |
| 3f | 540 宽度下选「新手」以后，面板是收起的，棋盘上看不到「点击 e4」这条任务 | 教学模式下，任务条放在棋盘上方（与面板开合无关） |
| 3g | 每日计划做完一步，醒目的「下一题」进的是一步杀，不是计划里的下一步 | 每日计划进行中，「下一题」跟着计划走 |

**验收**：每条一个 e2e 断言。3c 在 1400×900 和 540×900 下，都要能点到弹窗的最后一行。

---

## 4 · 误导人的数字和名字（走查，部分核过）

- **「进步」显示的是失手率，却没有写明**（progress.js:119 算的是 `m/(m+s)`，核过）。
  两步杀失手 1 次、解出 0 次显示「本周 100%」，一步杀全对显示「0%」，一般人会读成正确率。
  改成正确率。
- **回顾里 `?! · ? · ??` 三个标签只配了两个数**：默认「存疑标注」关着，数值只给
  「1 · 3」（app.js:4441），读起来对错位了。标签跟随这个开关。
- **开局题的题名和实际走的线对不上**：练习沿整棵开局树走，走 1.e4 也被接受，标题却仍写
  「A01 尼姆佐-拉森」；开局书练习走完 Rossolimo 分支，提示「背谱完成 · C54 Italian」，
  意大利那条也被记成已背。「背下来 0/9」要重载才更新。改成按实际走完的那条线记账、起名，
  计数即时刷新。
- **开局书的线名整条是英文**（「C54 Italian Game: Classical Variation, …」）：7.5 的族名
  本地化没有覆盖这条显示路径。
- **导入的 1-0 棋局，状态栏写「黑方认输」**，这是编出来的原因；对阵栏显示「玩家 1 /
  玩家 2」，没有用棋谱里的名字。
- **自动收进错题以后**，「把这一手收进错题」按钮照旧显示。

---

## 5 · CI 与发布：从 3 小时降到 1 小时以内（审计，核过）

### 依据

- 8 个 e2e 文件里共 29 处 `page.click("#pick-cancel").catch(() => {})`，都没有设 timeout。
  测试预先写了设置，所以引导窗根本不会弹出，每次 click 都要等满 Playwright 默认的 30 秒
  才失败。本机逐个套件计时：这部分合计约 2500 秒，占本机总时长 97 分钟的 40% 以上；
  在 library、board、audio 三个套件里占 65–75%。
- 固定的 `waitForTimeout` 合计约 15 分钟，主要在 layout 和 content 两个套件。
- `release.yml` 在一个 job 里串行跑两个引擎，这一步 2 小时 36 分；整次发布 3 小时 11 分。

### 做什么

1. 所有 `#pick-cancel` 点击改成先 `isVisible` 再点（flows 已经是这个写法）。预计每个
   引擎从约 70 分钟降到约 40 分钟。
2. `release.yml` 的浏览器测试改成按 引擎 × 套件 的矩阵并行 job；平台构建也和测试并行
   派发，草稿要等测试全绿才发布。
3. layout、content 里的长固定等待改成 `waitForFunction`。

### 验收

- checks 的单个浏览器 job ≤ 45 分钟。
- 一次发布 ≤ 60 分钟。
- 失败时的行为不变：任何一项红了，都不会打 tag、不会发布。

---

## 6 · 真引擎测试补齐（审计）

按用户影响排序，给 `test-engine-flows-e2e` 补这几个场景：

1. 持续分析 + 棋谱库分析同时进行（即 §1a 的回归测试）。
2. 「再深一遍」：400ms 预算，reviseMines 会撤回或修改题目。
3. 较高档位的人机加棋钟：easy/normal/hard 走 UCI_Elo 和 movetime，棋钟会压缩 maxMs。
4. multipv=3 的分析与持续分析。
5. 从 FEN / SetUp 起始的分析（黑先、着法编号）。
6. 补 7.5 欠下的：场景 4（持续分析）和 6（教学对练）的先红后绿。

**验收**：每个新场景都先红后绿，都在两套引擎上跑。

---

## 7 · 真壳自检扩到引擎以外

**依据**：7.5 的自检只让引擎走一步。真机清单 J–O 六节（6.0–7.3 的原生存档、桥、菜单、
`.pgn` 关联、棋谱库）从来没有人在真机上走过；最后一次真机走查停在 5.2.0。其中有几项
不需要人，可以放进 CI 自检。

**做什么**：`CHESS_SELFTEST` 模式下，页面在引擎那一步之后，再做这几件事：

- `chess.appdataWrite` 写一个标记，再 `appdataRead` 读回来比对；
- 用 `zero://` 懒加载 `chunk-eco.js`，查一个开局名；
- `selftest-app.mjs` 连续启动两次：第一次写一个 localStorage 标记，第二次读回来，
  确认重启后状态还在。

报告里每一项分别给出结果，任何一项失败，整步就红。

**验收**：故意让其中一项坏掉（例如把 chunk 的文件名改错），两个平台都红，而且报告里
能看出是哪一项。

---

## 8 · 打磨（走查）

- 4 条课题文字里有原样的 `**深**`（第 2 课、第 45 课）。
- 「h8（黑方那侧的角落）」有歧义：点 a8 会被判错。
- 每日计划的步骤名「上一课新的」意思不清。
- 英文界面里，着法菜单按钮的 title 还是中文「着法操作」；英文的 acc-line 写 Precision，
  回顾里写 Accuracy，用词不统一。日文「ここから指し直す」在按钮里挤成两行。
- 盲人用的 `#board-live` 在做题模式里一直停在旧对局的最后一着，不播报对方的应着。
- 战术题「把车引离底线」计入的母题是 fork，界面上显示的却是「引离」。
- 开局书本身有 5 组、9 条同一开局两种写法（卡罗-卡恩 / 卡罗-康 等，7.5 §8 登记过），
  统一掉，把 test-eco 里的例外表清零。

---

## 9 · 这一版仍然不做的

- 签名、公证、自动更新：缺证书。
- `app.js` 拆分（9807 行）：§1–§4 的修法都很局部，这一版不重构。
- Lichess 题库、残局查表。
- 单个巨大对局挪进 Worker 解析：没有用户碰到过，代价不小。
- 窄窗棋盘下方的留白。

---

## 10 · 顺序

1. §5 的 `#pick-cancel` 修法先做：改动最小，而且后面每一轮 CI 都能快 40%。
2. §1 P0 三条，每条带回归测试。
3. §2 这一类修干净，在 WebKit 上验证。
4. §3、§4。
5. §6 真引擎场景（§1a 的那条随 §1 一起做）。
6. §7 自检扩展，§5 的发布流水线并行化。
7. §8 打磨。
8. 收尾：版本号、release notes、README、落地记录。

**发布前仍然需要**：A0 以及真机清单 J–O，至少在一台 Mac 或一台 Windows 上走一次。
§7 能减少这件事的分量，但替代不了它。

---

## 11 · 落地记录（7.6.0）

这一版由五条并行的工作线完成，最后合在一起跑 CI。下面按计划的节写，做到的和没做到
的都写。

### §1 P0

- **1a**：`liveAllowed()` 加上 `!store.session.libRun`。「再深一遍」也用 `libRun`，
  所以同一处一起挡住。两种分析在 `finally` 里如果发现 `liveOn` 仍为真，就 `sync()`
  一次，让持续分析接上。flows 新场景：先开持续分析，再跑棋谱库分析，中途翻一步棋。
  不带修复时 90 秒后仍停在「正在分析第 2/3 局 · 2/11」，带修复时三局跑完。
- **1b**：新增 `pgnLocked()`。分析进行中，存为变着、预览里「从这里试」、着法菜单
  里的升为主线 / 删除分支 / 编辑注释都置灰；棋盘走子、悔棋、从这里重下、提交注释被拒
  并提示（新键 `msg.pgnLocked`）。把 `pgnLocked` 固定成 false 时，flows 里 3 条红：
  棋盘走子成功，精析被悄悄吞掉，没有任何提示。
- **1c**：新模块 `analysis-store.js`，新键 `chess.v1.analyses`，按「起始 FEN + 着法」
  存，最多 24 条，序列化后不超过 60 万字符，浅的不覆盖深的。启动恢复、对局历史、打开
  PGN、从棋谱库打开，四条路径都先查它，引擎 go 计数为 0。棋盘上分析完一局，如果库里
  有着法和起始局面都相同的一局、而且新结果更深又没有空洞，就写回那一条的 `an`，并照
  「再深一遍」的规则修正挖出来的错题。
  - 回写按内容匹配：用「打开 PGN」载入的同一局也会回写。这是有意的。
  - 已知遗留：设置本来就是人机模式时，从对局历史载入的那一刻会先向引擎发一次搜索，
    随即取消。这是之前就有的行为，这一版没有修。

### §2 点击期间 DOM 被重建

- 棋谱库：状态行挪到按钮下方的 `#lib-status`；`#lib-body` 只剩计数行，原地改字。
  `lib.noneClaimed` 三语里的「在下面」改成「在上面」。
- `#live-line`：按 multipv 行数只建一次骨架，之后每帧只改文字，容器高度只增不减。
- `#pv-line`：内容不变就不重建。
- 测试方法：`scripts/lib/held-click.mjs` 按住 450–600ms 再松开，期间逐帧量被按元素
  中心的位移、数它里面的 DOM 变动、记录 click 有没有落到它身上。

| 场景（Chromium，桩引擎） | 修前 | 修后 |
|---|---|---|
| 名字 → 分析（1100×1000） | 按钮上移 15.9px | 0px |
| 持续分析 multipv=3 时连按「新局」10 次 | 最大位移 116px，0/10 生效 | 0px，10/10 |
| 分析中按主变小块 | 节点被换掉，点击丢失 | 节点不变，点中 |

**WebKit 上还有第二个原因**，是合并后的 CI 抓到的：Chromium 全绿，WebKit 上按钮
0px 位移、节点未换，点击仍然丢了。原因是名字框失焦触发的重绘用 `textContent` 把按钮
里的文字又写了一遍，换掉了按下时命中的那个文本节点。WebKit 记下的按下节点可以是文本
节点，它被摘下后，松开时找不到共同祖先，click 就不派发。修法：按钮与状态文字只在
变了时才写；名字框失焦引起的重绘推迟到指针松开之后；输入停顿 600ms 也提交名字。
held-click 加了「按住期间 DOM 变动为 0」的断言（修前：名字 → 分析 1 处，暂停分析
8 处）。

**没做到的**：分析进行中「分析」按钮的文字「停止 9/61 → 10/61」会变宽，同一行后面
的按钮会横向挪一个数字宽。不在审计列出的四处里，没动。

### §3 主路径断点

3a–3g 都做了，每条都有 e2e 断言，并在旧代码上跑出过红：

- 3a：四种 locale 首启，旧代码存的是 `learn / normal`。
- 3b：三种情况（从记录页打开库局、设置页换语言、隐藏期间改窗口尺寸），旧代码画布 1×1。
- 3c：1400×900 与 540×900 下，旧代码卡片 0–1055 / 900，关闭按钮与最后一行都点不到。
  toast 的 z-index 从 40 调到 70，高于所有弹窗。
- 3d：新增 `leaveTrainer()`；用户在确认框里拒绝替换棋盘时，原来的题 / 课放回去。
- 3e：旧代码停在第 80 半着，现在停在第 55 半着。**和计划不同**：计划只说从诊断打开
  要切到对局页，实际做成了所有库局打开都切过去（包括从列表打开），行为更一致。
- 3f：新增 `#task-strip`。宽度 820px 以下始终显示，宽窗在面板收起时显示；它的高度
  从棋盘可用高度里扣，540×900 下棋盘仍是 528px。
- 3g：旧代码点「下一题」进的是 `puzzle/m1`，现在进计划的第 2 步。

### §4 / §8 数字、名字与打磨

- 「进步」改成正确率 `s/(m+s)`，文案写明「本周正确率」，最弱的排前。
- `?! · ? · ??` 跟随存疑标注开关。
- 开局题新增 `opCurrent()`：题目要求、思路、完成提示、「接实战」、记账都读实际走完的
  那条线；「背下来 N/M」当场刷新。
- 开局书线名走 `ChessEco.localName`。
- 导入棋局的结果只写「1-0 · 白方胜」这样的结果；在应用里真的认输或同意和棋时才写原因
  （新键 `st.result10`、`st.result01`、`st.resultDraw`）。对阵栏用棋谱名字，「?」时
  退回「玩家 1 / 玩家 2」。
- 收错题按钮在已收时写明并置灰。
- 课文里 16 处原样的 `**` 去掉，test-chess 断言 `**` 只能出现在会渲染成粗体的段落里。
- 「h8」提示改成「白方视角的右上角」；每日计划步骤名改成「学一节新课」；英文着法菜单
  的 title 跟着语言换（原因是着法行按签名复用节点，签名里没有语言）；英文与日文的精准度
  用词统一；「重下」按钮三语都改短到一行（英文最后是 Resume）；做题 / 教学时读屏播报
  题板自己的着法；手写母题优先于推导，引离、消除防守者、过载不再记成捉双。
- 开局书五组两种写法统一为：卡罗-卡恩、王翼印度、四马开局、彼得罗夫防御、后兵开局；
  王兵开局一并统一，另有三条按族名对齐。test-eco 的例外表清空，并断言旧写法不再出现。
  线的 id 由 ECO 与着法生成，改名不影响进度。

**没做到的**：
- 这些新断言大多没有专门在旧代码上跑出红。实测过红的只有英文「Replay from here」在
  按钮里是两行这一条。
- 英文开局名里 Defence / Defense 并存，没有统一。
- 英文课文里的单星号 `*斜体*` 仍会原样显示。
- 诊断弹窗里各行的数值比 114px 的数值列宽，在 460px 宽的卡片里会被截掉。

### §5 CI 与发布

- **§5.1**：31 处 `#pick-cancel` 点击限时 1.5 秒（ff1cb1c）。本地 clock 套件 129 秒 →
  48 秒；CI 上单个 WebKit job 约 70 分钟 → 28 分钟（改成并行之前）。
- **§5.2**：checks.yml 与 release.yml 的浏览器测试都拆成 引擎 × 5 组 的并行 job，
  引擎检查拆成 8 个 job。发布的 publish job `needs` 其余全部 job；平台构建经
  `workflow_call` 与测试同时跑，包经 artifact 交给 publish。test-chess 新增两条守卫：
  `test:engine:sample` 的每条命令原样出现在 release.yml 里；publish job 必须 needs
  其余每一个 job。两条都做过反向验证。拆分之后，PR 上的浏览器 job 每组 3–8 分钟。
- **§5.3 没做到**：content 套件里的固定等待改成条件等待（8d5b759），在 WebKit 上两次
  都在「走对之后进到下一题」红了。这项改动只省约 48 秒，不值得冒 WebKit 时序的风险，
  已撤回（07e5885）。layout 里最大的固定等待同时兜着教学的首步演示和桩引擎的启动失败
  横幅，换成条件等待会改变测量时的页面状态，没改。
- **第一次并行发布**（run 36136865248）：其余 job 全绿，`test-mines` 红在「次佳着差距
  中位数 = 25（区间 30–600）」。这段代码与 7.5.0 相比没有改动；它只在发布里跑，PR 上
  不跑。同一份代码本地连跑三次，结果是 33 / 78 / 86，记录值是 116（5 个局面）。
  六个局面的中位数，本身就会晃到 30 以下。下限改成 10，仍然能抓住「次佳着通常和最佳
  着一样好」（那时中位数接近 0）。修好后重跑发布。

### §6 真引擎测试

flows 从 6 个场景增到 14 个，全部在两套引擎上跑，每个新场景都先红后绿：

| 场景 | 怎么弄红 | 红的表现 |
|---|---|---|
| 持续分析 + 棋谱库 | 不带 1a 的修复 | 90 秒后仍停在第 2/3 局 |
| 分析中锁棋谱 | `pgnLocked` 恒为 false | 3 条 FAIL，精析被悄悄吞掉 |
| 分析存盘 | 关掉恢复 | 2 条 FAIL |
| 精析回写库 | 关掉回写 | budget 仍是 200，假题还在 |
| 再深一遍 | 跳过 `mineLibraryGame` | 撤销 0 道 |
| 人机高档棋钟 | 不按棋钟压缩 | movetime 900（应为 666） |
| 多主变 | 分析时不传 multipv | alt=0 |
| FEN 黑先 | 按奇偶判断走子方 | 标注为空 |
| 7.5 欠下的：持续分析 | `liveAllowed` 恒为 false | FAIL |
| 7.5 欠下的：教学对练 | 引擎不应着 | 15 秒内没有应着 |

棋谱库场景也改走真实路径：点输入框、打字、直接点「分析」，不再手动派发 change。

### §7 真壳自检 v2

报告格式 `{ok, checks:{engine, appdata, chunk, restart}}`，各项用 `pass` 而不用 `ok`
（main.zig 按报告里有没有 `"ok":true` 决定退出码，所以 zig 零改动）。
`selftest-app.mjs` 连续启动两次，第二次必须读回第一次写下的 localStorage 标记。

| | macOS（WKWebView） | Windows（WebView2） |
|---|---|---|
| 正常（8d5b759） | 绿：四项都过；第二次启动读回了标记；第一步 d4，引擎 1979 ms | 绿：四项都过 |
| eco 分块改成不存在的文件名（3a30905，临时提交，已撤回） | 红：`FAIL: chunk —— chunk-eco-missing.js loaded without ECO_BY_KEY`，其余三项照常通过 | 红：`各项：engine ok，appdata ok，chunk FAIL，restart ok` |

**顺带看到的**：`zero://` 对不存在的文件并不报 404，而是返回了某个内容，所以只有
「加载后检查全局表」这一步能抓到。自检正是这样检查的。

### 数字

界面键 917 → 921：`msg.pgnLocked`、`st.result10`、`st.result01`、`st.resultDraw`。
存档键 13 → 14（`chess.v1.analyses`）。真引擎流程场景 6 → 14。

### 发布前仍然需要

A0 以及真机清单 J–O，至少在一台 Mac 或一台 Windows 上走一次。§7 的自检已经在两个
平台的真 WebView 里查了引擎、存档、懒加载和重启，但机器是 GitHub 的，不是用户的。
